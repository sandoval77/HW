#include <iostream>
#include <unistd.h> // For sleep()

int main() {
    while (true) {
        std::cout << "Running..." << std::endl;
        sleep(1); // Sleep for 1 second to make the output more manageable
    }
    return 0;
}
